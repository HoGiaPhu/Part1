var tip;
var bill = 275
if (bill > 50 && bill < 300) {
    tip = bill * 0.15;
    console.log("The bill are " + bill + " the tip are " + tip + " and total are: " + (bill + tip));
} else{
    tip = bill * 0.2;
    console.log("The bill are " + bill + " the tip are " + tip + " and total are: " + (bill + tip));
}


var bill = 40
if (bill > 50 && bill < 300) {
    tip = bill * 0.15;
    console.log("The bill are " + bill + " the tip are " + tip + " and total are: " + (bill + tip));
} else{
    tip = bill * 0.2;
    console.log("The bill are " + bill + " the tip are " + tip + " and total are: " + (bill + tip));
}


var bill = 430
if (bill > 50 && bill < 300) {
    tip = bill * 0.15;
    console.log("The bill are " + bill + " the tip are " + tip + " and total are: " + (bill + tip));
} else{
    tip = bill * 0.2;
    console.log("The bill are " + bill + " the tip are " + tip + " and total are: " + (bill + tip));
}